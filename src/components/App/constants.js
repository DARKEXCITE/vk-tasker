export const panel = {
    desks: 'desks',
    columns: 'columns'
}

// Модификации формы
export const modes = {
    button: 'button',
    form: 'form'
}

// Статус поля ввода
export const statuses = {
    default: 'default',
    error: 'error'
}
